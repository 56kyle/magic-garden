"""Module defining pet mutations in Magic Garden."""
import enum


class PetMutation(enum.Enum):
    """Enum defining pet mutations in Magic Garden."""
    NORMAL = "normal"
    GOLD = "gold"
    RAINBOW = "rainbow"
    MAX_WEIGHT = "max_weight"
